name: Commit message helper

on:
  workflow_dispatch:

jobs:
  hint:
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: echo "Commit message helper is currently disabled until it has a valid implementation."
